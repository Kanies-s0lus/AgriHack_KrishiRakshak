To Setup Server:
    - Run in terminal:
        pip install -r requirements.txt

        python create_db.py
        python server.py
    - Run in second terminal:
        python app.py (for debug mode)
            or
        flask run 
